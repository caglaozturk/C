#include <stdio.h> 
#include <conio.h> 
#include <string.h> 
  int main() { 
       int i,j, temp,k; 
       char str[100]="caglaozturk";
       for(i=0; str[i]!='\0'; i++){     } 
       for(j=i; j>=0; j--) { 
            printf("%c",str[j]); 
       } 
   
       getch(); 
       return 0; 
  } 
